<Window x:Class="AEPluginInstaller.Views.InstallSummaryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:AEPluginInstaller.Services.Localization"
        Title="{loc:T Key=dlg.summary.title}"
        Height="520" Width="560"
        MinHeight="400" MinWidth="500"
        Background="{StaticResource BgBrush}" Foreground="{StaticResource TextBrush}"
        FontFamily="Segoe UI" Icon="/Assets/logo.ico"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="24,22,24,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="{loc:T Key=dlg.summary.title}" FontSize="18" FontWeight="Bold"/>
            <TextBlock x:Name="SubtitleText" Foreground="{StaticResource TextMutedBrush}"
                       FontSize="12" Margin="0,4,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                CornerRadius="8">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="8">
                <ItemsControl x:Name="PluginsList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource BgBrush}"
                                    CornerRadius="6" Margin="4,2" Padding="12,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding Name}"
                                                   FontSize="13" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding StatusText}"
                                                   FontSize="11"
                                                   Foreground="{StaticResource TextMutedBrush}"
                                                   Margin="0,2,0,0"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" Text="{Binding SizeText}"
                                               FontSize="12"
                                               Foreground="{StaticResource TextMutedBrush}"
                                               VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="2" Margin="0,14,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{loc:T Key=dlg.summary.total_size}"
                           Foreground="{StaticResource TextMutedBrush}"/>
                <TextBlock Grid.Column="1" x:Name="TotalSizeText" FontWeight="SemiBold"/>
            </Grid>
            <Grid Grid.Row="1" Margin="0,4,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" x:Name="FreeSpaceLabel"
                           Foreground="{StaticResource TextMutedBrush}"/>
                <TextBlock Grid.Column="1" x:Name="FreeSpaceText"/>
            </Grid>
            <TextBlock Grid.Row="2" x:Name="NotEnoughHint"
                       Text="{loc:T Key=dlg.summary.not_enough}"
                       Foreground="{StaticResource DangerBrush}"
                       FontSize="12" Margin="0,8,0,0"
                       Visibility="Collapsed"/>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="{loc:T Key=dlg.cancel}"
                    Style="{StaticResource GhostButton}" Padding="18,8" Margin="0,0,8,0"
                    Click="CancelBtn_Click"/>
            <Button Content="{loc:T Key=dlg.summary.start}"
                    Style="{StaticResource PrimaryButton}" Padding="22,8"
                    Click="StartBtn_Click"/>
        </StackPanel>
    </Grid>
</Window>
